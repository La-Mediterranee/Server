

retrieve porducts with category:
db.collection("products").where("categories", "array-contains", category)

## DB Collections
/company
    name: string
    address: map = {
        country: string
        street: string
        city: string;
        zip: string
    }
    opening-hours: array
    owner: string
    logo: {
        small: string;
        big: string;
    }

/food 
    id = category name so we avoid duplication
        subcollection: products

/products 
    ID: string;
    price: number;
    translations: {
        [lang]: {
            name: string;
            desc: string;
        } 
    }
    orders: number; // added to get the 10 most ordered products as the most popular
    tags: string[] every category is an id   // or make every product belong to one category (string)
    
    /*
    reviews: map = {
        value: number; // 1-5 // increment on add with FieldValue.increment
        count: number; // 400 reviews  // recalculate on every add (currentValue + new review/ reviewsCount),
        reviews: map = {
            userID: value: number 1-5
        }
    } 
    */

/customers
    customClaims: {
        stripeID: string;
    }

    uid: string;

/orders
    ID: string;
    timestamp: timestamp;
    user: User;
    lineItems: [{
        ID: string;
        name: string;
        toppings: ToppingOption[]
        itemTotal: 
    }]


// I don't think I will include reviews
/reviews
    id: productid (sku)
        userID: id; // one time review or change this to a map and to include userID and rating
